###############################
# PurpleMusicApp .env.example #
###############################

###############################
# FRONTEND-EMBEDDED VARIABLES #
###############################
# These MUST use the REACT_APP_ prefix to be injected into the React build (CRA requirement).
# Provide safe (public) values only. Do NOT place secrets (service keys) here.

# Public backend API base URL (your deployed backend origin)
REACT_APP_API_URL=https://purplemusic-backend.onrender.com

# Supabase public (anon) keys – safe to expose in frontend
REACT_APP_SUPABASE_URL=https://ofkfygqrfenctzitigae.supabase.co
REACT_APP_SUPABASE_ANON_KEY=PASTE_PUBLIC_ANON_KEY

########################################
# BACKWARD COMPATIBILITY (OPTIONAL)     #
########################################
# Legacy variable names without prefix (the app still falls back to these locally).
# You can keep them or remove once all environments use the REACT_APP_ versions.
SUPABASE_URL=https://ofkfygqrfenctzitigae.supabase.co
SUPABASE_ANON_KEY=PASTE_PUBLIC_ANON_KEY

# Opcionális saját API token (ha egyszer bevezetsz auth header-t)
API_KEY=OPTIONAL_FRONT_TOKEN

# CSAK BACKEND! (NE tegyél ilyet Netlify / publikus környezetbe)
# Ezeket a Render-en állítod be Environment vars résznél.
SUPABASE_SERVICE_KEY=__DO_NOT_COMMIT__
PI_API_KEY=__DO_NOT_COMMIT__

########################################
# LOCAL DEVELOPMENT TIPS                #
########################################
# 1. Copy this file to .env (gitignored) and fill real values.
# 2. The build script verify-env.js requires the REACT_APP_* variants.
# 3. If you need to run a build with placeholders, you can temporarily set dummy
#    values (the app will likely fail at runtime for authenticated features).
# 4. NEVER commit real keys; keep only examples here.

# Megjegyzés: valódi .env fájl NEM kerül gitbe, lásd .gitignore